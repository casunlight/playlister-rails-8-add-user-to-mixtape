<%= form_for(@mixtape_user, :url => mixtape_users_path(@mixtape)) do |f| %>
  <%= f.select :user_id , @users.collect { |u| [u.email, u.id] } %>
  <%= f.hidden_field :mixtape_id, value: @mixtape.id %>
  <div class="actions">
  <br>
  <%= f.submit "Add User" %>
  </div>
<% end %>